[package]
name = "bamboo-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Vietnamese input method transformation engine (Rust port of BambooEngine/bamboo-core)"

[dependencies]
once_cell = "1"
regex = "1"
